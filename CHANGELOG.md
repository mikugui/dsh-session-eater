# 更新记录

## 未发布

- **新增**：**删除前确认会写出这个会话消耗了多少 token**。询问语默认变成
  「你确定要吃掉这个消耗了 `1.2M` token 的会话吗？」，下面再补一行副信息（`62 轮 · 缓存命中 97%`）。
  - token 数来自**内核自己的会话投影缓存**（`storages/session_projcache/sessions/<id>.json` 的
    `tokenUsage.totals`），与界面同源。特意**不去解** `session.v3.jsonl.zstd` —— 那是多帧 zstd
    （5MB 的会话要解出 19MB 文本、花 300ms），而且里面 `data.usage` 是「每轮一条、上下文递增」的，
    求和会算出 5 亿这种荒谬值（正确读法是取最后一条，还得自己拼 4 个 bucket）。
  - 总量 = 未缓存输入 + 输出 + 缓存读 + 缓存写；缓存读通常占大头，所以副信息行标了命中占比。
  - 读不到时（空白会话 / 投影还没生成）退回不带数字的那条询问语，不写"未知"。
  - 宿主新增 `POST /usage`；客户端在 drop 时 await 一次并缓存（宿主读的是小 JSON，几十毫秒）。
    **宿主侧改动要重启 `dsh web` 才生效** —— 重启前弹窗照常工作，只是不带数字。
  - 可改文案 16 段：新增「确认弹窗标题（带用量）」，里面的 `{tokens}` 会被替换成实际用量。

## 0.5.0

**这一版把"用起来"打磨了一遍**：删会话多了一道确认（可在设置里关）、设置页能直达也能折叠、
外观重做了一轮；另外修掉两个"看起来像坏了、其实刷新就好"的真问题。

### 交互

- **新增**：**删除前确认**。拖上去松手不再直接删，而是在左侧会话列表里弹出
  「确定要删除这个会话吗？」并列出会话名 + 删除/取消两个按钮；按 `Esc` 或点取消则**一个请求都不发**。
  默认焦点在**取消**上（回车不会误删）。设置里有 **删除前确认** 开关（默认开，关掉＝回到"松手即删"），
  确认框那三段文案（询问语 / 删除 / 取消）也可自定义 —— 可改文案从 12 段增到 **15 段**。
- **新增**：**点侧栏药丸（会话框里的图标）直接进「设置 → 会话喂鱼」**（键盘 Tab + 回车/空格同样可用），
  不用再"点设置 → 找分区"两步；鼠标悬停提示「点击打开会话喂鱼」。
- **移除**：侧边栏药丸行右侧那个「撤销最近吃掉的一条」按钮。它读的是 localStorage 台账，
  而台账本来就要跨刷新保留 —— 于是刷新/重启后按钮一直挂在那儿，看着像个删不掉的按钮。
  撤销只保留两条路：回执上的撤销（内存态，刷新自然消失）+ 设置里的 12 条台账（可持久）。

### 设置页

- **新增**：**可折叠区块**。「文案」（15 段）和「最近吃掉」（最多 12 条）太长，现在默认收起、
  点标题展开，标题右侧给 `15 段` / `2 条` 这样的摘要；**收起时正文不渲染**（不是藏起来）。
  展开状态记在 `config.ui.open` 里，刷新后保持。
- **精简**：「删除前确认」区块只留**功能名 + 开关**，去掉说明文字。
- **美化**：整体收拾了一遍 ——
  - 卡片：圆角 14px、内边距 14/16、柔和描边 + 1px 投影
  - 每个区块标题前加 **3px 渐变竖条**，扫读时一眼分辨分组
  - **开关改成胶囊拨杆**（原生 checkbox 用 `appearance:none` 重画，a11y 与点击行为不变）
  - 折叠标题：箭头固定 `▸`、展开时 CSS 转 90°，摘要做成小胶囊，展开带 180ms 淡入
  - 滑块数值改成等宽胶囊；输入框统一 28px 高、聚焦绿色描边；分段控件选中态用品牌绿
  - 台账每行加悬停高亮与行间细分隔线；空态改成居中的「🐟 还没有吃掉任何会话」
  - 底部「恢复全部默认」用分隔线断开；预览台加极淡棋盘格（能看出自定义图的透明区域）
  - 配色全走主题变量（tint 用 `label-primary` 调），**浅色主题不会发黑**

### 修复

- **修复**：**样式自愈**。注入的 `<style>` 原本只在 `apply()` 时挂一次，而它挂在 `ctx.effect`
  的清理函数上 —— 客户端半边热重载时会被摘掉，若那次重载没跑完，页面就一直没样式
  （药丸/投放区退回默认块级布局）。现在用 `MutationObserver` 盯 `<head>`，样式不在就补一次
  （`ensureCss` 幂等，不会死循环）。这个坑是从别人的插件上实测出来的：表情包选择面板因此从
  **4 列网格** 退化成 **774×1800 的一列竖排**，刷新才好。
- **修复**：`/status` 的 `version` 之前写死成 `0.3.0`（包早就到 0.4.x 了），排障时差点把
  "宿主其实是新版"误判成"宿主没重载"；现在直接读 `package.json`。客户端诊断桥的 `version`
  同样写死过，现在改成启动时问一次宿主 `/status` 并用 getter 暴露 —— 两半边都不会再漂移。
- **修复**：`inject` 里补上 `sessions`。之前 `apply()` 跑得比该服务注册更早，导致"隐藏已吃掉会话"
  的过滤器被静默跳过 —— 现象是删掉后切到「单列表」视图，被吃的会话又整排冒回来。

### 工具与文档

- **新增**：`scripts/submit-market.mjs` —— 一条命令投 DSH 插件市场（加 topic / 补无版本号 tarball /
  fork + 建分支 + 写 yml + 开 PR），带 `--dry-run` 与 `--prepare`（先做不受 1 天门槛限制的部分）。
- **新增**：`tests/verify-market-entry.mjs` —— 投稿前自检，对照市场 CI 规则核 entry/category/引号/tarball。
- **新增**：`tests/loop.mjs` —— 把咀嚼动画导成循环动图（透明 GIF / 动画 WebP + 透明 PNG 序列），
  做视频片头/封面用。
- **新增**：`screenshots.json` —— 插件市场详情页展示你仓库里的截图。
- **文档**：新增「排障：删不掉 / 拖了没反应 / 界面样式裸奔」——先 F5、再打 `/status`、再查
  日志里的 `duplicate loader entry id`；并修掉「本机当前的实际装法」那节的自相矛盾
  （既让人抄 `- insert:` 到用户 patch 层、又警告别加进 bundles，两边都写会让 loader 直接起不来）。

## 0.4.1

- **修复**：`scripts/*.mjs --dry-run` 之前会**先校验凭据**，没有令牌就直接报错 ——
  跟"先看看会推哪些文件"这个用途自相矛盾。现在 dry-run 完全离线，不需要任何凭据。
- **文档**：新增「不需要令牌的发版路径」（`git tag` + 网页 Draft a new release）。

## 0.4.0

- **新增**：侧边栏药丸行**最右侧**的撤销按钮（与图标同一行对齐），一键撤回最近吃掉的一条；
  台账为空时不显示，鼠标悬停会说明要撤的是哪条。
- **新增**：设置页「最近吃掉」台账（最多 12 条，存浏览器本地），每条可单独撤销 ——
  不必再去抓那条几秒就消失的回执。
- **新增**：界面文案全部可自定义（12 段），留空即不显示；默认值改为「想吃大白饭 / 啊啊啊」等。
- **修复**：「删了之后刷新会话又回来了」。真正的根因是**宿主进程里会话对象仍然存活**，
  新页面拉 baseline 时它被重新塞回客户端列表；「按工作区」视图靠账目过滤看不出来，
  但切到**「单列表」视图**就会整排冒回来。现在插件会把台账里的会话从客户端列表里持续摘掉。
- **修复**：带撤销的回执不再 4 秒自动消失（改为常驻 + ✕ 关闭）。
- **修复**：回执加了宽度上限与不可压缩的按钮，长标题不会再把撤销按钮挤出屏幕。
- **修复**：自定义图标不是正方形时，嘴按 `object-fit: contain` 后的**实际显示矩形**定位，
  不再飘到留白上。
- **修复**：`inject` 里补上 `sessions` —— 之前 `apply()` 跑得比该服务注册更早，
  导致隐藏已吃掉会话的过滤器被静默跳过。

## 0.3.0

- **新增**：设置页（设置 → 会话喂鱼）：形象来源（跟随余额挂件 / 自定义文件 / 拖放 / 网址）、
  预览里拖拽校准嘴的位置、投放区与药丸头像尺寸、咀嚼周期与腮红/点头开关。
- **新增**：空白会话（列表里的「新会话」）会被拒绝并说明原因 ——
  删了 DSH 也会立刻再建一个，看起来就像"没删掉"。
- **修复**：嘴从平面椭圆改成手绘 SVG（唇线用原画描边色、口腔渐变、舌头高光），
  并把「张嘴」改成下颚下垂、闭嘴收成唇线的咀嚼动画。

## 0.2.0

- **新增**：嘴部开合动画与腮红脉动。
- **修复**：投放区在指针进入的瞬间把自己卸载掉（`phase` 变成 `over` 后不再满足显示条件），
  改成「已武装 / 悬停 / 正在吃 / 刚吃完」全程留在场上。

## 0.1.0

- 首个可用版本：把会话拖到余额挂件的形象上即删除（移入回收站），
  支持撤销回执、宿主侧 `/delete` `/restore` 路由与回收站 manifest。
